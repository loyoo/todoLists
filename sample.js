//前面通过todoListPre.js已经创建todoList对象

todoList.saveData(domId) = function(){
  //把todoList.json保存到cookie,使用key为todoList-domId
};
todoList.getData(domId) = function(){
  //从cookie获取'todoList-domId'节点,再返回文本
  return json;
};